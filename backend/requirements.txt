# FastAPI and Uvicorn
fastapi==0.115.0
uvicorn[standard]==0.32.0

# Database
sqlalchemy==2.0.23
alembic==1.12.1
# psycopg2-binary==2.9.9  # For PostgreSQL (enable if needed for production)
aiosqlite==0.19.0  # For SQLite (development environment)

# Pydantic (validation)
pydantic==2.10.0
pydantic-settings==2.6.0
email-validator==2.1.0  # Required for EmailStr validation

# Authentication and Security
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1  # Use 4.0.1 for compatibility with passlib 1.7.4 (5.0.0+ is incompatible)
python-multipart==0.0.6

# CORS
fastapi-cors==0.0.6

# Environment variables
python-dotenv==1.0.0

# Date and time
python-dateutil==2.8.2

# Excel processing (for document generation)
openpyxl==3.1.2

# WebSocket
websockets==12.0

# Testing
pytest==7.4.3
pytest-asyncio==0.21.1
pytest-cov==4.1.0
httpx==0.25.1

# Code quality
black==23.11.0
flake8==6.1.0
mypy==1.7.1
